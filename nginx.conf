events {
    worker_connections 1024;
}

http {
    upstream app {
        server localhost:3001;
        server localhost:3002;
    }

    server {
        listen 3000;

        location / {
            proxy_pass http://app;
        }
    }
}